services:
  paito:
    image: ghcr.io/gofhir/paito-fhir-server:latest
    container_name: paito-fhir
    restart: unless-stopped
    ports:
      - "3500:3500"
      - "3501:3501"
    environment:
      GOFHIR_STORAGE_DRIVER: mongodb
      GOFHIR_MONGODB_URI: "mongodb://paito:paito@mongodb:27017/paito?authSource=admin"
      GOFHIR_DATABASE_HOST: postgres
      GOFHIR_DATABASE_PORT: "5432"
      GOFHIR_DATABASE_USER: paito
      GOFHIR_DATABASE_PASSWORD: paito
      GOFHIR_DATABASE_NAME: paito
      GOFHIR_SERVER_PORT: "3500"
    depends_on:
      postgres:
        condition: service_healthy
      mongodb:
        condition: service_healthy
    volumes:
      - paito_data:/app/data
    networks:
      - paito-network

  postgres:
    image: postgres:16-alpine
    container_name: paito-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: paito
      POSTGRES_PASSWORD: paito
      POSTGRES_DB: paito
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U paito -d paito"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - paito-network

  mongodb:
    image: mongo:7
    container_name: paito-mongodb
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: paito
      MONGO_INITDB_ROOT_PASSWORD: paito
      MONGO_INITDB_DATABASE: paito
    volumes:
      - mongodb_data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - paito-network

volumes:
  postgres_data:
  mongodb_data:
  paito_data:

networks:
  paito-network:
    driver: bridge
